1. Написати функцію для розв’язування квадратного рівняння. 
Функція отримує коефіціенти квадратного рівняння та вказівники(або посилання) на змінні, у яких потрібно розмістити корені рівняння.
Функція повертає кількість різних коренів рівняння(0-коренів немає, 1- два однакові корені, 2 -різних корені). 
Перевірити роботу функції.

2. Створити функцію  для обчислення довжини кола та площі круга за радіусом кола. Результати розмістити у параметрах -вказівниках.

3. Створити функцію, яка повертає  число та суму парних елементів  у масиві через  параметри посилання або  параметри вказівники.. 


------------Додаткові завдання---------------------------------------------------
4. *Визначити функцію, яка повертає посилання на найбільший елемент масиву. 
У функції main() за допомогою цієї функції замінити максимальний елемент(зайти по посиланню) на 0.
	refMaxElement(arr, size) = 0;

5. * Визначити функцію, яка отримує  два вказівники : початкову адресу у масиві(start) та кінцеву адресу(end).
Функція повертає адресу першого додатного елемента, що розміщений між вказівниками  start та end.
Якщо додатний елемент не  знайдено - повертати нульовий вказівник(nullptr).
int * findPositive(int * start, int *  end);

За допомогою функції findPositive збільшити на 1  всі додатні елементи у одновимірному масиві.
* Скористатися функцією у циклі, доки шукаються додатні зібльшувати(+1) і продовжувати пошук.
